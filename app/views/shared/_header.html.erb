<header>
  <div class="nav">
    <div class="direct-top">
      <%= link_to 'knots', root_path, class: 'root'%>
    </div>
    <ul class="lists">
    <% if user_signed_in? %>
      <li class="list"><%= link_to '相談する', new_topic_path, class: 'new' %></li>
      <li class="list"><%= link_to 'マイページ', user_path(current_user.id), class: 'my' %></li>
    <% else %>
      <li class="list"><%= link_to 'ログイン', new_user_session_path, class: 'rog-in' %></li>
      <li class="list"><%= link_to '新規登録', new_user_registration_path, class: 'sign-in' %></li>
    <% end %>
    </ul>
  </div>
</header>